# AWS SES Sender IAM Example - Environment Variables
#
# Copy this file to .env and fill in your values if you want to customize
# the example. By default, the example uses values defined in the YAML template.

# Email Identity
SENDER_EMAIL=sender@example.com

# Domain Identity
SENDER_DOMAIN=example.com
MAIL_FROM_DOMAIN=mail.example.com

# IAM Resources
IAM_GROUP_NAME=ses-senders
IAM_POLICY_NAME=ses-sender-policy
IAM_USER_NAME=ses-sender-user

# AWS Region
AWS_REGION=us-east-1

# Email recipient for testing (should be verified in SES sandbox)
TEST_RECIPIENT_EMAIL=recipient@example.com

