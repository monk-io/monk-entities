# AWS Cognito Authentication Example - Environment Variables
# Copy this file to .env and fill in your actual values

# AWS Configuration
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key

# Google OAuth Configuration (Optional - for social login)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Application Configuration
# These are automatically populated by Monk when running via monk run
# But you can set them manually for standalone development

# Frontend Environment Variables (React)
REACT_APP_REGION=us-east-1
REACT_APP_USER_POOL_ID=us-east-1_XXXXXXXXX
REACT_APP_CLIENT_ID=XXXXXXXXXXXXXXXXXXXXXXXXXX
REACT_APP_DOMAIN=your-domain-prefix
REACT_APP_IDENTITY_POOL_ID=us-east-1:XXXXXXXX-XXXX-XXXX-XXXX-XXXXXXXXXXXX

# Backend Environment Variables (Express.js)
USER_POOL_ID=us-east-1_XXXXXXXXX
REGION=us-east-1
PORT=3001

# Optional: Logging Configuration
LOG_LEVEL=info
NODE_ENV=development

# Optional: CORS Configuration
CORS_ORIGIN=http://localhost:3000

# Notes:
# - When using 'monk run cognito-auth-example', these values are automatically injected
# - For standalone development, copy actual values from your AWS Cognito setup
# - Google OAuth credentials are only needed if you want to test social login
# - Never commit real credentials to version control
