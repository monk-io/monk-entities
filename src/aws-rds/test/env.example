# AWS RDS Entity Test Environment Variables
# Copy this file to .env and fill in your actual values

# Test configuration
MONKEC_VERBOSE=true
TEST_TIMEOUT=1800000

# Optional: Specific AWS region for testing
# AWS_DEFAULT_REGION=us-east-1

# Optional: Test database configuration
# TEST_DB_USERNAME=testuser
# TEST_DB_PASSWORD=TestPassword123!

# Backup test configuration
TEST_MYSQL_BACKUP_PASSWORD=YourSecureBackupPassword123!

# Note: AWS credentials should be configured through AWS CLI or environment variables
# The AWS builtin module will use the standard AWS credential chain