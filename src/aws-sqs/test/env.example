# AWS SQS Queue Entity Test Environment Configuration
# Copy this file to .env and fill in your actual values

# Optional: Test configuration
# MONKEC_VERBOSE=true
# TEST_TIMEOUT=300000

# Note: AWS credentials should be configured through the Monk runtime
# The built-in AWS module will automatically use configured credentials 