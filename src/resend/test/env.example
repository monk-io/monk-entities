# Resend API key (do not commit real values).
# Get one at https://resend.com/api-keys — create with Full Access permission
# (the integration tests create/delete domains, api-keys, and webhooks).
RESEND_API_TOKEN=

# Optional
MONKEC_VERBOSE=true
TEST_TIMEOUT=300000
