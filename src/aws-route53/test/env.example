# AWS credentials (configured via monk cluster providers)
# No additional secrets needed — Route 53 uses standard AWS credentials

# Optional: override test domain name
# TEST_DOMAIN_NAME=monk-test-route53.example.com
