# MongoDB Atlas Test Environment Variables
# Copy this file to .env and fill in your actual values

# Required: MongoDB Atlas Service Account Credentials
# Format: clientId:clientSecret (colon-separated, no spaces)
# Get from: https://cloud.mongodb.com → Organization Settings → Access Manager → Service Accounts
# Create a service account → Generate Token → Copy both Client ID and Client Secret
# Example: MONGODB_ATLAS_TOKEN=mdb_client_abc123:secret_xyz789
MONGODB_ATLAS_TOKEN=your_client_id:your_client_secret

# Optional: Test configuration
# MONKEC_VERBOSE=true
# TEST_TIMEOUT=540000

# Backup test configuration (optional - retrieved during test execution)
# SNAPSHOT_ID=your-snapshot-id-from-list-snapshots
# TARGET_PROJECT_ID=target-project-id-for-restore
# RESTORE_JOB_ID=your-restore-job-id