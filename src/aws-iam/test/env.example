# AWS IAM Policy Entity Test Environment Variables
# Copy this file to .env and update with your actual secrets

# Test secrets only (AWS credentials are automatically injected into builtin aws module)
# Add any test-specific sensitive data here if needed

# Example: S3 bucket name for testing (if environment-specific)
# TEST_S3_BUCKET=your-test-bucket-name

# Example: AWS Account ID (if different per environment) 
# AWS_ACCOUNT_ID=123456789012

# Development settings
MONKEC_VERBOSE=true 