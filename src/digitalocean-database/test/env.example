# DigitalOcean Database Integration Test Configuration
# DigitalOcean credentials are configured through Monk runtime (monk cluster providers)
# No API tokens needed in .env
#
# Prerequisites:
#   sudo monk cluster provider add -p digitalocean

# Optional test configuration
# MONKEC_VERBOSE=true
# TEST_TIMEOUT=1800000
