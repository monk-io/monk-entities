// Generated by MonkEC - targeting Goja runtime
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var __publicField = (obj, key, value) => __defNormalProp(obj, typeof key !== "symbol" ? key + "" : key, value);

// input/stripe/stripeBase.ts
var stripe_base_exports = {};
__export(stripe_base_exports, {
  StripeEntity: () => StripeEntity
});
module.exports = __toCommonJS(stripe_base_exports);
var import_base = require("monkec/base");
var import_http_client = require("monkec/http-client");
var import_secret = __toESM(require("secret"));
var StripeEntity = class extends import_base.MonkEntity {
  constructor() {
    super(...arguments);
    __publicField(this, "apiKey");
    __publicField(this, "httpClient");
    __publicField(this, "baseUrl", "https://api.stripe.com/v1");
  }
  before() {
    const key = import_secret.default.get(this.definition.secret_ref);
    if (!key) {
      throw new Error(`Failed to retrieve Stripe API key from secret: ${this.definition.secret_ref}`);
    }
    this.apiKey = key;
    this.httpClient = new import_http_client.HttpClient({
      baseUrl: this.baseUrl,
      headers: {
        "Authorization": `Bearer ${this.apiKey}`,
        "Accept": "application/json",
        "Content-Type": "application/x-www-form-urlencoded"
      },
      parseJson: true,
      stringifyJson: false
    });
  }
  makeRequest(method, path, formBody) {
    try {
      const body = formBody ? this.encodeForm(formBody) : void 0;
      const response = this.httpClient.request(method, path, body ? { body } : {});
      if (!response.ok) {
        throw new Error(`Stripe API error: ${response.statusCode} ${response.status} - ${response.data}`);
      }
      return response.data;
    } catch (error) {
      const msg = error instanceof Error ? error.message : String(error);
      throw new Error(`Stripe ${method} ${path} failed: ${msg}`);
    }
  }
  encodeForm(data) {
    const pairs = [];
    for (const [k, v] of Object.entries(data)) {
      if (v === void 0 || v === null) continue;
      pairs.push(encodeURIComponent(k) + "=" + encodeURIComponent(v));
    }
    return pairs.join("&");
  }
  deriveMode() {
    if (this.apiKey.startsWith("sk_test_")) return "test";
    if (this.apiKey.startsWith("sk_live_")) return "live";
    return this.apiKey.includes("test") ? "test" : "live";
  }
};
__publicField(StripeEntity, "readiness", { period: 10, initialDelay: 1, attempts: 12 });
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  StripeEntity
});
