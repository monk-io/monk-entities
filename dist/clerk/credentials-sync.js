
// Generated by MonkEC - targeting Goja runtime
var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });

// input/clerk/credentials.ts
const clerkBase = require("clerk/clerk-base");
const ClerkEntity = clerkBase.ClerkEntity;
const cli = require("cli");
var _Credentials = class _Credentials extends ClerkEntity {
  getEntityName() {
    return "clerk-credentials";
  }
  create() {
    const mode = this.deriveMode();
    let instanceId;
    try {
      const result = this.makeRequest("GET", "/instance");
      instanceId = result?.id || void 0;
    } catch {
      this.makeRequest("GET", "/users?limit=1");
      instanceId = void 0;
    }
    this.state = {
      instance_id: instanceId,
      mode,
      publishable_key: this.definition.publishable_key,
      secret_ref: this.definition.secret_ref,
      existing: false
    };
    cli.output(`Clerk credentials valid (mode: ${mode})`);
  }
  update() {
    this.create();
  }
  delete() {
    cli.output("Clerk credentials entity has no remote resources to delete");
  }
  checkReadiness() {
    return Boolean(this.state?.secret_ref);
  }
};
__name(_Credentials, "Credentials");
var Credentials = _Credentials;



function main(def, state, ctx) {
  const entity = new Credentials(def, state, ctx);
  return entity.main(ctx);
}
