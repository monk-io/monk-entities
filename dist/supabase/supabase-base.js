// Generated by MonkEC - targeting Goja runtime
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var __publicField = (obj, key, value) => __defNormalProp(obj, typeof key !== "symbol" ? key + "" : key, value);

// input/supabase/supabaseBase.ts
var supabase_base_exports = {};
__export(supabase_base_exports, {
  SupabaseEntity: () => SupabaseEntity
});
module.exports = __toCommonJS(supabase_base_exports);
var import_base = require("monkec/base");
var import_http_client = require("monkec/http-client");
var import_cli = __toESM(require("cli"));
var import_secret = __toESM(require("secret"));
var SupabaseEntity = class extends import_base.MonkEntity {
  constructor() {
    super(...arguments);
    /**
     * Personal Access Token for Supabase Management API access
     */
    __publicField(this, "apiToken");
    /**
     * HTTP client configured for Supabase Management API
     */
    __publicField(this, "httpClient");
    /**
     * Base URL for Supabase Management API
     */
    __publicField(this, "baseUrl", "https://api.supabase.com");
  }
  /**
   * Initialize authentication and HTTP client before any operations
   */
  before() {
    const secretRef = this.definition.secret_ref || "supabase-api-token";
    const token = import_secret.default.get(secretRef);
    if (!token) {
      throw new Error(`Failed to retrieve API token from secret: ${secretRef}`);
    }
    this.apiToken = token;
    this.httpClient = new import_http_client.HttpClient({
      baseUrl: this.baseUrl,
      headers: {
        "Authorization": `Bearer ${this.apiToken}`,
        "Accept": "application/json",
        "Content-Type": "application/json"
      },
      parseJson: true,
      stringifyJson: true,
      timeout: 3e4
    });
  }
  /**
   * Standard start implementation for Supabase entities
   */
  start() {
    import_cli.default.output(`Starting Supabase operations for: ${this.getEntityName()}`);
  }
  /**
   * Standard stop implementation for Supabase entities  
   */
  stop() {
    import_cli.default.output(`Stopping Supabase operations for: ${this.getEntityName()}`);
  }
  /**
   * Helper method to make authenticated HTTP requests with consistent error handling
   */
  makeRequest(method, path, body) {
    try {
      const fullUrl = `${this.baseUrl}${path}`;
      import_cli.default.output(`\u{1F527} Making ${method} request to: ${fullUrl}`);
      if (body && method !== "GET" && method !== "DELETE") {
        import_cli.default.output(`\u{1F4E6} Request body: ${JSON.stringify(body, null, 2)}`);
      }
      const headers = {
        "Authorization": `Bearer ${this.apiToken}`,
        "Accept": "application/json"
      };
      if (method !== "DELETE" && (method !== "GET" || body)) {
        headers["Content-Type"] = "application/json";
      }
      const requestOptions = { headers };
      if (method !== "GET" && method !== "DELETE" && body) {
        requestOptions.body = body;
      }
      const response = this.httpClient.request(method, path, requestOptions);
      import_cli.default.output(`\u{1F4E1} Response status: ${response.statusCode}`);
      if (!response.ok) {
        import_cli.default.output(`\u274C Error response body: ${response.data}`);
        throw new Error(`Supabase API error: ${response.statusCode} ${response.status} - ${response.data}`);
      }
      let responseData = response.data;
      if (typeof responseData === "string") {
        try {
          responseData = JSON.parse(responseData);
        } catch (e) {
        }
      }
      return responseData;
    } catch (error) {
      import_cli.default.output(`\u{1F50D} makeRequest caught error: ${typeof error}`);
      let errorMessage = "Unknown error";
      if (error instanceof Error) {
        errorMessage = error.message;
      } else if (typeof error === "string") {
        errorMessage = error;
      } else if (typeof error === "object" && error !== null) {
        try {
          errorMessage = JSON.stringify(error);
        } catch {
          errorMessage = String(error);
        }
      } else {
        errorMessage = String(error);
      }
      throw new Error(`Supabase ${method} request to ${path} failed: ${errorMessage}`);
    }
  }
  /**
   * Helper method to check if a resource exists by making a GET request
   * Returns the resource data if it exists, null otherwise
   */
  checkResourceExists(path) {
    try {
      return this.makeRequest("GET", path);
    } catch (error) {
      return null;
    }
  }
  /**
   * Helper method to handle resource deletion with proper existing resource checks
   */
  deleteResource(path, resourceName) {
    if (this.state.existing) {
      import_cli.default.output(`${resourceName} wasn't created by this entity, skipping delete`);
      return;
    }
    try {
      this.makeRequest("DELETE", path);
      import_cli.default.output(`Successfully deleted ${resourceName}`);
    } catch (error) {
      const errorMessage = error instanceof Error ? error.message : "Unknown error";
      if (errorMessage.includes("404") || errorMessage.includes("not found")) {
        import_cli.default.output(`\u26A0\uFE0F  ${resourceName} not found, may have been already deleted`);
        return;
      }
      throw new Error(`Failed to delete ${resourceName}: ${errorMessage}`);
    }
  }
};
__publicField(SupabaseEntity, "readiness", { period: 15, initialDelay: 2, attempts: 20 });
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  SupabaseEntity
});
