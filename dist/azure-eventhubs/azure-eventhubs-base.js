// Generated by MonkEC - targeting Goja runtime
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var __publicField = (obj, key, value) => __defNormalProp(obj, typeof key !== "symbol" ? key + "" : key, value);

// input/azure-eventhubs/azureEventhubsBase.ts
var azure_eventhubs_base_exports = {};
__export(azure_eventhubs_base_exports, {
  AzureEventHubsEntity: () => AzureEventHubsEntity
});
module.exports = __toCommonJS(azure_eventhubs_base_exports);
var import_base = require("monkec/base");
var import_cli = __toESM(require("cli"));
var import_azure = __toESM(require("cloud/azure"));
var _AzureEventHubsEntity = class _AzureEventHubsEntity extends import_base.MonkEntity {
  constructor() {
    super(...arguments);
    __publicField(this, "apiVersion", "2024-01-01");
    __publicField(this, "baseUrl", "https://management.azure.com");
  }
  /**
   * Standard start implementation for Azure Event Hubs entities
   */
  start() {
    import_cli.default.output(`Starting Azure Event Hubs operations for: ${this.getEntityName()}`);
  }
  /**
   * Standard stop implementation for Azure Event Hubs entities  
   */
  stop() {
    import_cli.default.output(`Stopping Azure Event Hubs operations for: ${this.getEntityName()}`);
  }
  /**
   * Redact sensitive fields from an object for safe logging
   */
  redactSensitiveFields(obj) {
    if (obj === null || obj === void 0) {
      return obj;
    }
    if (typeof obj !== "object") {
      return obj;
    }
    if (Array.isArray(obj)) {
      return obj.map((item) => this.redactSensitiveFields(item));
    }
    const result = {};
    for (const [key, value] of Object.entries(obj)) {
      if (_AzureEventHubsEntity.SENSITIVE_FIELDS.some(
        (field) => key.toLowerCase().includes(field.toLowerCase())
      )) {
        result[key] = "[REDACTED]";
      } else if (typeof value === "object" && value !== null) {
        result[key] = this.redactSensitiveFields(value);
      } else {
        result[key] = value;
      }
    }
    return result;
  }
  /**
   * Helper method to make authenticated Azure API requests with consistent error handling
   */
  makeAzureRequest(method, path, body) {
    const url = `${this.baseUrl}${path}`;
    const headers = {
      "Content-Type": "application/json"
    };
    import_cli.default.output(`\u{1F527} Making ${method} request to: ${url}`);
    const bodyString = body ? JSON.stringify(body) : void 0;
    if (body && (method === "PUT" || method === "POST" || method === "PATCH")) {
      const redactedBody = this.redactSensitiveFields(body);
      import_cli.default.output(`\u{1F4E6} Request body: ${JSON.stringify(redactedBody)}`);
    }
    let response;
    switch (method.toUpperCase()) {
      case "GET":
        response = import_azure.default.get(url, { headers });
        break;
      case "PUT":
        response = import_azure.default.put(url, { headers, body: bodyString });
        break;
      case "POST":
        response = import_azure.default.post(url, { headers, body: bodyString });
        break;
      case "DELETE":
        response = import_azure.default.delete(url, { headers });
        break;
      case "PATCH":
        response = import_azure.default.do(url, { method: "PATCH", headers, body: bodyString });
        break;
      default:
        throw new Error(`Unsupported HTTP method: ${method}`);
    }
    import_cli.default.output(`\u{1F4E1} Response status: ${response.statusCode}`);
    if (response.body) {
      try {
        const parsedBody = JSON.parse(response.body);
        const redactedBody = this.redactSensitiveFields(parsedBody);
        import_cli.default.output(`\u{1F4E1} Response body: ${JSON.stringify(redactedBody)}`);
      } catch {
        import_cli.default.output(`\u{1F4E1} Response body: ${response.body}`);
      }
    } else {
      import_cli.default.output(`\u{1F4E1} Response body: `);
    }
    if (response.error) {
      import_cli.default.output(`\u274C Error response: ${response.error}`);
    }
    return response;
  }
  /**
   * Build the resource path for Azure API calls (namespace level)
   */
  buildNamespacePath(namespaceName) {
    return `/subscriptions/${this.definition.subscription_id}/resourceGroups/${this.definition.resource_group_name}/providers/Microsoft.EventHub/namespaces/${namespaceName}?api-version=${this.apiVersion}`;
  }
  /**
   * Build the resource path for Event Hub resources
   */
  buildEventHubPath(namespaceName, eventHubName) {
    return `/subscriptions/${this.definition.subscription_id}/resourceGroups/${this.definition.resource_group_name}/providers/Microsoft.EventHub/namespaces/${namespaceName}/eventhubs/${eventHubName}?api-version=${this.apiVersion}`;
  }
  /**
   * Build the resource path for consumer group resources
   */
  buildConsumerGroupPath(namespaceName, eventHubName, consumerGroupName) {
    return `/subscriptions/${this.definition.subscription_id}/resourceGroups/${this.definition.resource_group_name}/providers/Microsoft.EventHub/namespaces/${namespaceName}/eventhubs/${eventHubName}/consumergroups/${consumerGroupName}?api-version=${this.apiVersion}`;
  }
  /**
   * Build the resource path for authorization rules
   */
  buildAuthorizationRulePath(namespaceName, ruleName) {
    return `/subscriptions/${this.definition.subscription_id}/resourceGroups/${this.definition.resource_group_name}/providers/Microsoft.EventHub/namespaces/${namespaceName}/authorizationRules/${ruleName}?api-version=${this.apiVersion}`;
  }
  /**
   * Build the resource path for listing authorization rules keys
   */
  buildListKeysPath(namespaceName, ruleName) {
    return `/subscriptions/${this.definition.subscription_id}/resourceGroups/${this.definition.resource_group_name}/providers/Microsoft.EventHub/namespaces/${namespaceName}/authorizationRules/${ruleName}/listKeys?api-version=${this.apiVersion}`;
  }
  /**
   * Check if a resource exists by making a GET request
   * Returns the resource data if it exists, null otherwise
   */
  checkResourceExists(path) {
    const result = this.checkResourceExistsWithStatus(path);
    return result.resource;
  }
  /**
   * Check if a resource exists by making a GET request
   * Returns detailed status including whether it was a definitive 404 or another error
   */
  checkResourceExistsWithStatus(path) {
    try {
      const response = this.makeAzureRequest("GET", path);
      if (response.statusCode === 404) {
        return { resource: null, notFound: true };
      }
      if (response.error) {
        return { resource: null, notFound: false, error: response.error };
      }
      if (response.body) {
        return { resource: JSON.parse(response.body), notFound: false };
      }
      return { resource: null, notFound: false, error: "Empty response body" };
    } catch (error) {
      const errorMsg = error instanceof Error ? error.message : "Unknown error";
      import_cli.default.output(`\u26A0\uFE0F  Error checking if resource exists: ${errorMsg}`);
      return { resource: null, notFound: false, error: errorMsg };
    }
  }
  /**
   * Helper method to handle resource deletion with proper existing resource checks
   */
  deleteResourceByPath(path, resourceName) {
    if (this.state.existing) {
      import_cli.default.output(`${resourceName} wasn't created by this entity, skipping delete`);
      return;
    }
    try {
      const response = this.makeAzureRequest("DELETE", path);
      if (response.error) {
        if (response.statusCode === 404) {
          import_cli.default.output(`\u26A0\uFE0F  ${resourceName} not found, may have been already deleted`);
          return;
        }
        throw new Error(`${response.error}, body: ${response.body}`);
      }
      import_cli.default.output(`\u2705 Successfully initiated deletion of ${resourceName}`);
    } catch (error) {
      const errorMessage = error instanceof Error ? error.message : "Unknown error";
      throw new Error(`Failed to delete ${resourceName}: ${errorMessage}`);
    }
  }
  /**
   * Parse Azure API response body safely
   */
  parseResponseBody(response) {
    if (!response.body) {
      return null;
    }
    try {
      return JSON.parse(response.body);
    } catch (_error) {
      import_cli.default.output(`\u26A0\uFE0F  Failed to parse response body as JSON: ${response.body}`);
      return null;
    }
  }
  /**
   * Check if response indicates success
   */
  isSuccessResponse(response) {
    return !response.error && response.statusCode !== void 0 && response.statusCode >= 200 && response.statusCode < 300;
  }
  /**
   * Format bytes to human-readable format
   */
  formatBytes(bytes) {
    if (bytes === 0) return "0 Bytes";
    const k = 1024;
    const sizes = ["Bytes", "KB", "MB", "GB", "TB"];
    const i = Math.floor(Math.log(bytes) / Math.log(k));
    return parseFloat((bytes / Math.pow(k, i)).toFixed(2)) + " " + sizes[i];
  }
};
// Event Hubs namespaces typically provision in 1-3 minutes
// Total timeout: 10s initial + (20 attempts × 15s) = ~5 minutes
__publicField(_AzureEventHubsEntity, "readiness", { period: 15, initialDelay: 10, attempts: 20 });
/**
 * Sensitive field names that should be redacted from logs
 */
__publicField(_AzureEventHubsEntity, "SENSITIVE_FIELDS", [
  "key",
  "connectionString",
  "accessKey",
  "primaryKey",
  "secondaryKey",
  "sasToken",
  "secret",
  "sharedAccessKey"
]);
var AzureEventHubsEntity = _AzureEventHubsEntity;
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  AzureEventHubsEntity
});
