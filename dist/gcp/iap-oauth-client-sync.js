
// Generated by MonkEC - targeting Goja runtime
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __knownSymbol = (name, symbol) => (symbol = Symbol[name]) ? symbol : Symbol.for("Symbol." + name);
var __typeError = (msg) => {
  throw TypeError(msg);
};
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
var __decoratorStart = (base) => [, , , __create(base?.[__knownSymbol("metadata")] ?? null)];
var __decoratorStrings = ["class", "method", "getter", "setter", "accessor", "field", "value", "get", "set"];
var __expectFn = (fn) => fn !== void 0 && typeof fn !== "function" ? __typeError("Function expected") : fn;
var __decoratorContext = (kind, name, done, metadata, fns) => ({ kind: __decoratorStrings[kind], name, metadata, addInitializer: (fn) => done._ ? __typeError("Already initialized") : fns.push(__expectFn(fn || null)) });
var __decoratorMetadata = (array, target) => __defNormalProp(target, __knownSymbol("metadata"), array[3]);
var __runInitializers = (array, flags, self, value) => {
  for (var i = 0, fns = array[flags >> 1], n = fns && fns.length; i < n; i++) flags & 1 ? fns[i].call(self) : value = fns[i].call(self, value);
  return value;
};
var __decorateElement = (array, flags, name, decorators, target, extra) => {
  var fn, it, done, ctx, access, k = flags & 7, s = !!(flags & 8), p = !!(flags & 16);
  var j = k > 3 ? array.length + 1 : k ? s ? 1 : 2 : 0, key = __decoratorStrings[k + 5];
  var initializers = k > 3 && (array[j - 1] = []), extraInitializers = array[j] || (array[j] = []);
  var desc = k && (!p && !s && (target = target.prototype), k < 5 && (k > 3 || !p) && __getOwnPropDesc(k < 4 ? target : { get [name]() {
    return __privateGet(this, extra);
  }, set [name](x) {
    return __privateSet(this, extra, x);
  } }, name));
  k ? p && k < 4 && __name(extra, (k > 2 ? "set " : k > 1 ? "get " : "") + name) : __name(target, name);
  for (var i = decorators.length - 1; i >= 0; i--) {
    ctx = __decoratorContext(k, name, done = {}, array[3], extraInitializers);
    if (k) {
      ctx.static = s, ctx.private = p, access = ctx.access = { has: p ? (x) => __privateIn(target, x) : (x) => name in x };
      if (k ^ 3) access.get = p ? (x) => (k ^ 1 ? __privateGet : __privateMethod)(x, target, k ^ 4 ? extra : desc.get) : (x) => x[name];
      if (k > 2) access.set = p ? (x, y) => __privateSet(x, target, y, k ^ 4 ? extra : desc.set) : (x, y) => x[name] = y;
    }
    it = (0, decorators[i])(k ? k < 4 ? p ? extra : desc[key] : k > 4 ? void 0 : { get: desc.get, set: desc.set } : target, ctx), done._ = 1;
    if (k ^ 4 || it === void 0) __expectFn(it) && (k > 4 ? initializers.unshift(it) : k ? p ? extra = it : desc[key] = it : target = it);
    else if (typeof it !== "object" || it === null) __typeError("Object expected");
    else __expectFn(fn = it.get) && (desc.get = fn), __expectFn(fn = it.set) && (desc.set = fn), __expectFn(fn = it.init) && initializers.unshift(fn);
  }
  return k || __decoratorMetadata(array, target), desc && __defProp(target, name, desc), p ? k ^ 4 ? extra : desc : target;
};
var __publicField = (obj, key, value) => __defNormalProp(obj, typeof key !== "symbol" ? key + "" : key, value);
var __accessCheck = (obj, member, msg) => member.has(obj) || __typeError("Cannot " + msg);
var __privateIn = (member, obj) => Object(obj) !== obj ? __typeError('Cannot use the "in" operator on this value') : member.has(obj);
var __privateGet = (obj, member, getter) => (__accessCheck(obj, member, "read from private field"), getter ? getter.call(obj) : member.get(obj));
var __privateSet = (obj, member, value, setter) => (__accessCheck(obj, member, "write to private field"), setter ? setter.call(obj, value) : member.set(obj, value), value);
var __privateMethod = (obj, member, method) => (__accessCheck(obj, member, "access private method"), method);

// input/gcp/iapOauthClient.ts
const base = require("monkec/base");
const action = base.action;
const gcpBase = require("gcp/gcp-base");
const GcpEntity = gcpBase.GcpEntity;
const secret = require("secret");
const cli = require("cli");
const iapCommon = require("gcp/iap-common");
const IAP_API_URL = iapCommon.IAP_API_URL;
var _getSecret_dec, _resetSecret_dec, _getInfo_dec, _a, _init;
var _IapOauthClient = class _IapOauthClient extends (_a = GcpEntity, _getInfo_dec = [action("get-info")], _resetSecret_dec = [action("reset-secret")], _getSecret_dec = [action("get-secret")], _a) {
  constructor() {
    super(...arguments);
    __runInitializers(_init, 5, this);
  }
  getEntityName() {
    return `GCP IAP OAuth Client "${this.definition.display_name}"`;
  }
  getParentUrl() {
    return `${IAP_API_URL}/projects/${this.projectId}/brands/${this.definition.brand_id}/identityAwareProxyClients`;
  }
  /**
   * Walk all pages of the OAuth-client list and return the first match by displayName.
   * GCP returns `nextPageToken` when more pages are available.
   */
  findClientByDisplayName(displayName) {
    let pageToken;
    do {
      const url = pageToken ? `${this.getParentUrl()}?pageToken=${encodeURIComponent(pageToken)}` : this.getParentUrl();
      const resp = this.get(url);
      const clients = resp.identityAwareProxyClients || [];
      const match = clients.find((c) => c.displayName === displayName);
      if (match) return match;
      pageToken = resp.nextPageToken ? String(resp.nextPageToken) : void 0;
    } while (pageToken);
    return void 0;
  }
  create() {
    const existing = this.findClientByDisplayName(this.definition.display_name);
    if (existing) {
      this.state.client_name = String(existing.name || "");
      const parts2 = this.state.client_name.split("/");
      this.state.client_id = parts2.length > 0 ? parts2[parts2.length - 1] : "";
      this.state.existing = true;
      cli.output(`Adopted existing IAP OAuth client: ${this.state.client_id}`);
      this.rotateSecretInternal();
      return;
    }
    const created = this.post(this.getParentUrl(), {
      displayName: this.definition.display_name
    });
    this.state.client_name = String(created.name || "");
    const parts = this.state.client_name.split("/");
    this.state.client_id = parts.length > 0 ? parts[parts.length - 1] : "";
    this.state.existing = false;
    if (created.secret) {
      secret.set(this.definition.secret_ref, String(created.secret));
      cli.output(`Created IAP OAuth client ${this.state.client_id} \u2014 secret stored at "${this.definition.secret_ref}"`);
    } else {
      cli.output(`Created IAP OAuth client ${this.state.client_id} \u2014 no secret in response (rotating to obtain one)`);
      this.rotateSecretInternal();
    }
  }
  update() {
    if (!this.state.client_name) {
      this.create();
    }
  }
  delete() {
    if (!this.state.client_name) return;
    if (this.state.existing) {
      cli.output("IAP OAuth client was pre-existing, skipping deletion");
      return;
    }
    this.httpDelete(`${IAP_API_URL}/${this.state.client_name}`);
    cli.output(`Deleted IAP OAuth client: ${this.state.client_id}`);
  }
  checkReadiness() {
    return Boolean(this.state.client_name);
  }
  checkLiveness() {
    return this.checkReadiness();
  }
  rotateSecretInternal() {
    if (!this.state.client_name) {
      throw new Error("Client not created yet");
    }
    const resp = this.post(`${IAP_API_URL}/${this.state.client_name}:resetSecret`, {});
    if (resp && resp.secret) {
      secret.set(this.definition.secret_ref, String(resp.secret));
      cli.output(`Rotated OAuth secret \u2014 updated "${this.definition.secret_ref}"`);
    } else {
      cli.output("Warning: resetSecret response did not contain a secret");
    }
  }
  getInfo(_args) {
    if (!this.state.client_name) {
      throw new Error("Client not created yet");
    }
    const info = this.get(`${IAP_API_URL}/${this.state.client_name}`);
    cli.output(`IAP OAuth Client: ${info.name}`);
    cli.output(`  Display name: ${info.displayName}`);
    cli.output(`  Client ID: ${this.state.client_id}`);
    cli.output(`  Secret stored at Monk secret: "${this.definition.secret_ref}"`);
  }
  resetSecret(_args) {
    this.rotateSecretInternal();
  }
  getSecret(_args) {
    cli.output(`OAuth client secret is stored in Monk secret: "${this.definition.secret_ref}"`);
    cli.output(`Retrieve with: sudo monk secrets get -g ${this.definition.secret_ref}`);
  }
};
_init = __decoratorStart(_a);
__decorateElement(_init, 1, "getInfo", _getInfo_dec, _IapOauthClient);
__decorateElement(_init, 1, "resetSecret", _resetSecret_dec, _IapOauthClient);
__decorateElement(_init, 1, "getSecret", _getSecret_dec, _IapOauthClient);
__decoratorMetadata(_init, _IapOauthClient);
__name(_IapOauthClient, "IapOauthClient");
__publicField(_IapOauthClient, "readiness", { period: 5, initialDelay: 1, attempts: 6 });
var IapOauthClient = _IapOauthClient;



function main(def, state, ctx) {
  const entity = new IapOauthClient(def, state, ctx);
  return entity.main(ctx);
}
