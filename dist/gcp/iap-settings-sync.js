
// Generated by MonkEC - targeting Goja runtime
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __knownSymbol = (name, symbol) => (symbol = Symbol[name]) ? symbol : Symbol.for("Symbol." + name);
var __typeError = (msg) => {
  throw TypeError(msg);
};
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
var __decoratorStart = (base) => [, , , __create(base?.[__knownSymbol("metadata")] ?? null)];
var __decoratorStrings = ["class", "method", "getter", "setter", "accessor", "field", "value", "get", "set"];
var __expectFn = (fn) => fn !== void 0 && typeof fn !== "function" ? __typeError("Function expected") : fn;
var __decoratorContext = (kind, name, done, metadata, fns) => ({ kind: __decoratorStrings[kind], name, metadata, addInitializer: (fn) => done._ ? __typeError("Already initialized") : fns.push(__expectFn(fn || null)) });
var __decoratorMetadata = (array, target) => __defNormalProp(target, __knownSymbol("metadata"), array[3]);
var __runInitializers = (array, flags, self, value) => {
  for (var i = 0, fns = array[flags >> 1], n = fns && fns.length; i < n; i++) flags & 1 ? fns[i].call(self) : value = fns[i].call(self, value);
  return value;
};
var __decorateElement = (array, flags, name, decorators, target, extra) => {
  var fn, it, done, ctx, access, k = flags & 7, s = !!(flags & 8), p = !!(flags & 16);
  var j = k > 3 ? array.length + 1 : k ? s ? 1 : 2 : 0, key = __decoratorStrings[k + 5];
  var initializers = k > 3 && (array[j - 1] = []), extraInitializers = array[j] || (array[j] = []);
  var desc = k && (!p && !s && (target = target.prototype), k < 5 && (k > 3 || !p) && __getOwnPropDesc(k < 4 ? target : { get [name]() {
    return __privateGet(this, extra);
  }, set [name](x) {
    return __privateSet(this, extra, x);
  } }, name));
  k ? p && k < 4 && __name(extra, (k > 2 ? "set " : k > 1 ? "get " : "") + name) : __name(target, name);
  for (var i = decorators.length - 1; i >= 0; i--) {
    ctx = __decoratorContext(k, name, done = {}, array[3], extraInitializers);
    if (k) {
      ctx.static = s, ctx.private = p, access = ctx.access = { has: p ? (x) => __privateIn(target, x) : (x) => name in x };
      if (k ^ 3) access.get = p ? (x) => (k ^ 1 ? __privateGet : __privateMethod)(x, target, k ^ 4 ? extra : desc.get) : (x) => x[name];
      if (k > 2) access.set = p ? (x, y) => __privateSet(x, target, y, k ^ 4 ? extra : desc.set) : (x, y) => x[name] = y;
    }
    it = (0, decorators[i])(k ? k < 4 ? p ? extra : desc[key] : k > 4 ? void 0 : { get: desc.get, set: desc.set } : target, ctx), done._ = 1;
    if (k ^ 4 || it === void 0) __expectFn(it) && (k > 4 ? initializers.unshift(it) : k ? p ? extra = it : desc[key] = it : target = it);
    else if (typeof it !== "object" || it === null) __typeError("Object expected");
    else __expectFn(fn = it.get) && (desc.get = fn), __expectFn(fn = it.set) && (desc.set = fn), __expectFn(fn = it.init) && initializers.unshift(fn);
  }
  return k || __decoratorMetadata(array, target), desc && __defProp(target, name, desc), p ? k ^ 4 ? extra : desc : target;
};
var __publicField = (obj, key, value) => __defNormalProp(obj, typeof key !== "symbol" ? key + "" : key, value);
var __accessCheck = (obj, member, msg) => member.has(obj) || __typeError("Cannot " + msg);
var __privateIn = (member, obj) => Object(obj) !== obj ? __typeError('Cannot use the "in" operator on this value') : member.has(obj);
var __privateGet = (obj, member, getter) => (__accessCheck(obj, member, "read from private field"), getter ? getter.call(obj) : member.get(obj));
var __privateSet = (obj, member, value, setter) => (__accessCheck(obj, member, "write to private field"), setter ? setter.call(obj, value) : member.set(obj, value), value);
var __privateMethod = (obj, member, method) => (__accessCheck(obj, member, "access private method"), method);

// input/gcp/iapSettings.ts
const base = require("monkec/base");
const action = base.action;
const gcpBase = require("gcp/gcp-base");
const GcpEntity = gcpBase.GcpEntity;
const cli = require("cli");
const iapCommon = require("gcp/iap-common");
const IAP_API_URL = iapCommon.IAP_API_URL;
const collectUpdateMaskPaths = iapCommon.collectUpdateMaskPaths;
const resolveIapResourceName = iapCommon.resolveIapResourceName;
var _showRaw_dec, _getInfo_dec, _a, _init;
var _IapSettings = class _IapSettings extends (_a = GcpEntity, _getInfo_dec = [action("get-info")], _showRaw_dec = [action("show-raw")], _a) {
  constructor() {
    super(...arguments);
    __runInitializers(_init, 5, this);
  }
  getEntityName() {
    return `GCP IAP Settings (${this.state.resource_name || this.definition.target_kind})`;
  }
  getResourceName() {
    return resolveIapResourceName(
      this.definition,
      this.state,
      this.projectId
    );
  }
  buildIapSettingsBody() {
    const body = { name: this.getResourceName() };
    if (this.definition.access_settings) {
      body.accessSettings = this.transformAccessSettings(
        this.definition.access_settings
      );
    }
    if (this.definition.application_settings) {
      body.applicationSettings = this.transformApplicationSettings(
        this.definition.application_settings
      );
    }
    return body;
  }
  transformAccessSettings(s) {
    const out = {};
    if (s.oauth_settings) {
      const oauth = {};
      if (s.oauth_settings.client_id !== void 0) oauth.clientId = s.oauth_settings.client_id;
      if (s.oauth_settings.client_secret !== void 0) oauth.clientSecret = s.oauth_settings.client_secret;
      if (s.oauth_settings.login_hint !== void 0) oauth.loginHint = s.oauth_settings.login_hint;
      if (s.oauth_settings.programmatic_clients !== void 0) oauth.programmaticClients = s.oauth_settings.programmatic_clients;
      out.oauthSettings = oauth;
    }
    if (s.cors_settings) {
      const cors = {};
      if (s.cors_settings.allow_http_options !== void 0) cors.allowHttpOptions = s.cors_settings.allow_http_options;
      out.corsSettings = cors;
    }
    if (s.reauth_settings) {
      const reauth = {};
      if (s.reauth_settings.method !== void 0) reauth.method = s.reauth_settings.method;
      if (s.reauth_settings.max_age !== void 0) reauth.maxAge = s.reauth_settings.max_age;
      if (s.reauth_settings.policy_type !== void 0) reauth.policyType = s.reauth_settings.policy_type;
      out.reauthSettings = reauth;
    }
    if (s.allowed_domains_settings) {
      const ads = {};
      if (s.allowed_domains_settings.domains !== void 0) ads.domains = s.allowed_domains_settings.domains;
      if (s.allowed_domains_settings.enable !== void 0) ads.enable = s.allowed_domains_settings.enable;
      out.allowedDomainsSettings = ads;
    }
    if (s.gcip_settings) {
      const g = {};
      if (s.gcip_settings.tenant_ids !== void 0) g.tenantIds = s.gcip_settings.tenant_ids;
      if (s.gcip_settings.login_page_uri !== void 0) g.loginPageUri = s.gcip_settings.login_page_uri;
      out.gcipSettings = g;
    }
    if (s.identity_sources !== void 0) out.identitySources = s.identity_sources;
    return out;
  }
  transformApplicationSettings(s) {
    const out = {};
    if (s.access_denied_page_settings) {
      const adp = {};
      if (s.access_denied_page_settings.access_denied_page_uri !== void 0) {
        adp.accessDeniedPageUri = s.access_denied_page_settings.access_denied_page_uri;
      }
      if (s.access_denied_page_settings.generate_troubleshooting_uri !== void 0) {
        adp.generateTroubleshootingUri = s.access_denied_page_settings.generate_troubleshooting_uri;
      }
      if (s.access_denied_page_settings.remediation_token_generation_enabled !== void 0) {
        adp.remediationTokenGenerationEnabled = s.access_denied_page_settings.remediation_token_generation_enabled;
      }
      out.accessDeniedPageSettings = adp;
    }
    if (s.cookie_domain !== void 0) out.cookieDomain = s.cookie_domain;
    if (s.attribute_propagation_settings) {
      const aps = {};
      if (s.attribute_propagation_settings.output_credentials !== void 0) {
        aps.outputCredentials = s.attribute_propagation_settings.output_credentials;
      }
      if (s.attribute_propagation_settings.expression !== void 0) {
        aps.expression = s.attribute_propagation_settings.expression;
      }
      if (s.attribute_propagation_settings.enable !== void 0) {
        aps.enable = s.attribute_propagation_settings.enable;
      }
      out.attributePropagationSettings = aps;
    }
    if (s.csm_settings) {
      const csm = {};
      if (s.csm_settings.rctoken_aud !== void 0) csm.rctokenAud = s.csm_settings.rctoken_aud;
      out.csmSettings = csm;
    }
    return out;
  }
  create() {
    const resourceName = this.getResourceName();
    const getUrl = `${IAP_API_URL}/${resourceName}:iapSettings`;
    const current = this.get(getUrl);
    if (current && (current.accessSettings || current.applicationSettings)) {
      this.state.prior_settings = {
        accessSettings: current.accessSettings,
        applicationSettings: current.applicationSettings
      };
      this.state.existing = true;
    } else {
      this.state.existing = false;
    }
    this.applySettings();
  }
  update() {
    this.applySettings();
  }
  delete() {
    const resourceName = this.state.resource_name;
    if (!resourceName) return;
    const url = `${IAP_API_URL}/${resourceName}:iapSettings?updateMask=accessSettings,applicationSettings`;
    if (this.state.prior_settings) {
      const body = { name: resourceName, ...this.state.prior_settings };
      this.patch(url, body);
      cli.output(`Restored prior IAP settings on ${resourceName}`);
    } else {
      const body = {
        name: resourceName,
        accessSettings: {},
        applicationSettings: {}
      };
      this.patch(url, body);
      cli.output(`Cleared IAP settings on ${resourceName}`);
    }
  }
  applySettings() {
    const resourceName = this.getResourceName();
    const body = this.buildIapSettingsBody();
    const paths = collectUpdateMaskPaths({
      accessSettings: body.accessSettings,
      applicationSettings: body.applicationSettings
    });
    if (paths.length === 0) {
      cli.output("No IAP settings to apply (definition has no access_settings or application_settings)");
      return;
    }
    const url = `${IAP_API_URL}/${resourceName}:iapSettings?updateMask=${paths.join(",")}`;
    this.patch(url, body);
    cli.output(`Applied IAP settings on ${resourceName}`);
    cli.output(`  updateMask: ${paths.join(",")}`);
  }
  checkReadiness() {
    return Boolean(this.state.resource_name);
  }
  checkLiveness() {
    return this.checkReadiness();
  }
  getInfo(_args) {
    if (!this.state.resource_name) {
      throw new Error("IAP settings not applied yet");
    }
    const info = this.get(`${IAP_API_URL}/${this.state.resource_name}:iapSettings`);
    cli.output(`IAP Settings for ${this.state.resource_name}:`);
    cli.output(JSON.stringify(info, null, 2));
  }
  showRaw(_args) {
    this.getInfo(_args);
  }
};
_init = __decoratorStart(_a);
__decorateElement(_init, 1, "getInfo", _getInfo_dec, _IapSettings);
__decorateElement(_init, 1, "showRaw", _showRaw_dec, _IapSettings);
__decoratorMetadata(_init, _IapSettings);
__name(_IapSettings, "IapSettings");
__publicField(_IapSettings, "readiness", { period: 5, initialDelay: 1, attempts: 3 });
var IapSettings = _IapSettings;



function main(def, state, ctx) {
  const entity = new IapSettings(def, state, ctx);
  return entity.main(ctx);
}
