// Generated by MonkEC - targeting Goja runtime
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// input/aws-ses/sesBase.ts
var ses_base_exports = {};
__export(ses_base_exports, {
  AWSSESEntity: () => AWSSESEntity
});
module.exports = __toCommonJS(ses_base_exports);
var import_base = require("monkec/base");
var import_aws = __toESM(require("cloud/aws"));
var import_cli = __toESM(require("cli"));

// input/aws-ses/common.ts
function parseSESError(response) {
  if (response.body) {
    const messageMatch = response.body.match(/<Message>(.*?)<\/Message>/);
    if (messageMatch) {
      return messageMatch[1];
    }
    const errorMatch = response.body.match(/<Error>(.*?)<\/Error>/s);
    if (errorMatch) {
      return errorMatch[1].replace(/<[^>]+>/g, " ").trim();
    }
  }
  return `HTTP ${response.statusCode}: ${response.status || "Unknown error"}`;
}

// input/aws-ses/sesBase.ts
var AWSSESEntity = class extends import_base.MonkEntity {
  get region() {
    return this.definition.region;
  }
  /**
   * Make a request to SES API v2
   */
  sesRequest(action, path, method = "GET", body) {
    const url = `https://email.${this.region}.amazonaws.com${path}`;
    const options = {
      service: "email",
      region: this.region,
      headers: {}
    };
    if (body) {
      options.headers["Content-Type"] = "application/json";
      options.body = body;
    }
    import_cli.default.output(`[SES API] ${method} ${url}`);
    if (body) {
      import_cli.default.output(`[SES API Request Body] ${body}`);
    }
    let response;
    try {
      if (method === "GET") {
        response = import_aws.default.get(url, options);
      } else if (method === "POST") {
        response = import_aws.default.post(url, options);
      } else if (method === "PUT") {
        response = import_aws.default.put(url, options);
      } else if (method === "DELETE") {
        response = import_aws.default.delete(url, options);
      } else {
        throw new Error(`Unsupported HTTP method: ${method}`);
      }
    } catch (error) {
      throw new Error(`SES API request failed: ${error instanceof Error ? error.message : String(error)}`);
    }
    import_cli.default.output(`[SES API Response] Status: ${response.statusCode}`);
    import_cli.default.output(`[SES API Response] Headers: ${JSON.stringify(response.headers || {})}`);
    if (response.body) {
      import_cli.default.output(`[SES API Response] Body: ${response.body}`);
    }
    if (response.error) {
      import_cli.default.output(`[SES API Response] Error: ${response.error}`);
    }
    if (response.statusCode >= 400) {
      const errorMessage = parseSESError(response);
      throw new Error(`SES API error (${action}): ${errorMessage}`);
    }
    if (response.body && response.statusCode >= 200 && response.statusCode < 300) {
      try {
        return JSON.parse(response.body);
      } catch (e) {
        return response;
      }
    }
    return response;
  }
  /**
   * Enable debug mode to print API requests and responses
   */
  get debug() {
    return false;
  }
};
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  AWSSESEntity
});
