
// Generated by MonkEC - targeting Goja runtime
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __knownSymbol = (name, symbol) => (symbol = Symbol[name]) ? symbol : Symbol.for("Symbol." + name);
var __typeError = (msg) => {
  throw TypeError(msg);
};
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
var __decoratorStart = (base) => [, , , __create(base?.[__knownSymbol("metadata")] ?? null)];
var __decoratorStrings = ["class", "method", "getter", "setter", "accessor", "field", "value", "get", "set"];
var __expectFn = (fn) => fn !== void 0 && typeof fn !== "function" ? __typeError("Function expected") : fn;
var __decoratorContext = (kind, name, done, metadata, fns) => ({ kind: __decoratorStrings[kind], name, metadata, addInitializer: (fn) => done._ ? __typeError("Already initialized") : fns.push(__expectFn(fn || null)) });
var __decoratorMetadata = (array, target) => __defNormalProp(target, __knownSymbol("metadata"), array[3]);
var __runInitializers = (array, flags, self, value) => {
  for (var i = 0, fns = array[flags >> 1], n = fns && fns.length; i < n; i++) flags & 1 ? fns[i].call(self) : value = fns[i].call(self, value);
  return value;
};
var __decorateElement = (array, flags, name, decorators, target, extra) => {
  var fn, it, done, ctx, access, k = flags & 7, s = !!(flags & 8), p = !!(flags & 16);
  var j = k > 3 ? array.length + 1 : k ? s ? 1 : 2 : 0, key = __decoratorStrings[k + 5];
  var initializers = k > 3 && (array[j - 1] = []), extraInitializers = array[j] || (array[j] = []);
  var desc = k && (!p && !s && (target = target.prototype), k < 5 && (k > 3 || !p) && __getOwnPropDesc(k < 4 ? target : { get [name]() {
    return __privateGet(this, extra);
  }, set [name](x) {
    return __privateSet(this, extra, x);
  } }, name));
  k ? p && k < 4 && __name(extra, (k > 2 ? "set " : k > 1 ? "get " : "") + name) : __name(target, name);
  for (var i = decorators.length - 1; i >= 0; i--) {
    ctx = __decoratorContext(k, name, done = {}, array[3], extraInitializers);
    if (k) {
      ctx.static = s, ctx.private = p, access = ctx.access = { has: p ? (x) => __privateIn(target, x) : (x) => name in x };
      if (k ^ 3) access.get = p ? (x) => (k ^ 1 ? __privateGet : __privateMethod)(x, target, k ^ 4 ? extra : desc.get) : (x) => x[name];
      if (k > 2) access.set = p ? (x, y) => __privateSet(x, target, y, k ^ 4 ? extra : desc.set) : (x, y) => x[name] = y;
    }
    it = (0, decorators[i])(k ? k < 4 ? p ? extra : desc[key] : k > 4 ? void 0 : { get: desc.get, set: desc.set } : target, ctx), done._ = 1;
    if (k ^ 4 || it === void 0) __expectFn(it) && (k > 4 ? initializers.unshift(it) : k ? p ? extra = it : desc[key] = it : target = it);
    else if (typeof it !== "object" || it === null) __typeError("Object expected");
    else __expectFn(fn = it.get) && (desc.get = fn), __expectFn(fn = it.set) && (desc.set = fn), __expectFn(fn = it.init) && initializers.unshift(fn);
  }
  return k || __decoratorMetadata(array, target), desc && __defProp(target, name, desc), p ? k ^ 4 ? extra : desc : target;
};
var __publicField = (obj, key, value) => __defNormalProp(obj, typeof key !== "symbol" ? key + "" : key, value);
var __accessCheck = (obj, member, msg) => member.has(obj) || __typeError("Cannot " + msg);
var __privateIn = (member, obj) => Object(obj) !== obj ? __typeError('Cannot use the "in" operator on this value') : member.has(obj);
var __privateGet = (obj, member, getter) => (__accessCheck(obj, member, "read from private field"), getter ? getter.call(obj) : member.get(obj));
var __privateSet = (obj, member, value, setter) => (__accessCheck(obj, member, "write to private field"), setter ? setter.call(obj, value) : member.set(obj, value), value);
var __privateMethod = (obj, member, method) => (__accessCheck(obj, member, "access private method"), method);

// input/resend/domain.ts
const base = require("monkec/base");
const action = base.action;
const cli = require("cli");
const resendBase = require("resend/resend-base");
const ResendEntity = resendBase.ResendEntity;
var _verify_dec, _getRecords_dec, _getInfo_dec, _a, _init;
var _ResendDomain = class _ResendDomain extends (_a = ResendEntity, _getInfo_dec = [action("get-info")], _getRecords_dec = [action("get-records")], _verify_dec = [action("verify")], _a) {
  constructor() {
    super(...arguments);
    __runInitializers(_init, 5, this);
  }
  create() {
    const existing = this.findByName(this.definition.name);
    if (existing?.id) {
      const full = this.fetchDomain(existing.id);
      this.state = {
        id: existing.id,
        name: existing.name || this.definition.name,
        status: full?.status || existing.status,
        region: full?.region || existing.region,
        records: full?.records || [],
        existing: true
      };
      this.flattenRecords();
      cli.output(
        `\u{1F4E7} Adopted existing Resend domain ${this.state.name} (${this.state.id})`
      );
      return;
    }
    const body = { name: this.definition.name };
    if (this.definition.region) body.region = this.definition.region;
    if (this.definition.custom_return_path)
      body.custom_return_path = this.definition.custom_return_path;
    if (this.definition.open_tracking !== void 0)
      body.open_tracking = this.definition.open_tracking;
    if (this.definition.click_tracking !== void 0)
      body.click_tracking = this.definition.click_tracking;
    const created = this.request("POST", "/domains", body);
    this.state = {
      id: created?.id,
      name: created?.name || this.definition.name,
      status: created?.status,
      region: created?.region,
      records: created?.records || [],
      existing: false
    };
    this.flattenRecords();
    cli.output(
      `\u2705 Created Resend domain ${this.state.name} (${this.state.id}) \u2014 status: ${this.state.status}`
    );
    if (this.state.records && this.state.records.length) {
      cli.output(
        `   Install ${this.state.records.length} DNS record(s) and run the verify action.`
      );
    }
  }
  update() {
    if (!this.state.id) {
      this.create();
      return;
    }
    const full = this.fetchDomain(this.state.id);
    if (full) {
      this.state.status = full.status || this.state.status;
      this.state.region = full.region || this.state.region;
      this.state.records = full.records || this.state.records;
      this.flattenRecords();
    }
  }
  delete() {
    if (!this.state.id) return;
    if (this.state.existing) {
      cli.output("Domain existed before this entity; skipping delete");
      return;
    }
    try {
      this.request("DELETE", `/domains/${this.state.id}`);
      cli.output(`\u{1F5D1}\uFE0F Deleted Resend domain ${this.state.id}`);
    } catch (e) {
      const msg = e?.message || String(e);
      if (msg.includes("404")) {
        cli.output(`Domain ${this.state.id} already gone`);
        return;
      }
      throw e;
    }
  }
  checkReadiness() {
    return Boolean(this.state.id);
  }
  getInfo() {
    if (!this.state.id) {
      cli.output("No domain yet");
      return;
    }
    const full = this.fetchDomain(this.state.id);
    cli.output(JSON.stringify(full || {}, null, 2));
  }
  getRecords() {
    if (!this.state.id) {
      cli.output("No domain yet");
      return;
    }
    const full = this.fetchDomain(this.state.id);
    cli.output(JSON.stringify(full?.records || [], null, 2));
  }
  verify() {
    if (!this.state.id) {
      cli.output("No domain yet");
      return;
    }
    this.request("POST", `/domains/${this.state.id}/verify`, {});
    const full = this.fetchDomain(this.state.id);
    if (full) {
      this.state.status = full.status || this.state.status;
      this.state.records = full.records || this.state.records;
      this.flattenRecords();
    }
    cli.output(
      `\u{1F501} Verification triggered. Current status: ${this.state.status}`
    );
  }
  flattenRecords() {
    const records = this.state.records || [];
    for (const r of records) {
      const kind = (r?.record || "").toUpperCase();
      const type = (r?.type || "").toUpperCase();
      if (kind === "DKIM" && type === "TXT") {
        this.state.dkim_name = r.name;
        this.state.dkim_value = r.value;
      } else if (kind === "SPF" && type === "MX") {
        this.state.spf_mx_name = r.name;
        this.state.spf_mx_value = r.value;
        this.state.spf_mx_priority = r.priority;
      } else if (kind === "SPF" && type === "TXT") {
        this.state.spf_txt_name = r.name;
        this.state.spf_txt_value = r.value;
      }
    }
  }
  fetchDomain(id) {
    try {
      return this.request("GET", `/domains/${id}`);
    } catch {
      return null;
    }
  }
  findByName(name) {
    try {
      const res = this.request("GET", "/domains");
      const list = res?.data || [];
      for (const d of list) {
        if (d?.name === name && d?.id) return d;
      }
      return null;
    } catch {
      return null;
    }
  }
};
_init = __decoratorStart(_a);
__decorateElement(_init, 1, "getInfo", _getInfo_dec, _ResendDomain);
__decorateElement(_init, 1, "getRecords", _getRecords_dec, _ResendDomain);
__decorateElement(_init, 1, "verify", _verify_dec, _ResendDomain);
__decoratorMetadata(_init, _ResendDomain);
__name(_ResendDomain, "ResendDomain");
__publicField(_ResendDomain, "readiness", { period: 5, initialDelay: 1, attempts: 6 });
var ResendDomain = _ResendDomain;



function main(def, state, ctx) {
  const entity = new ResendDomain(def, state, ctx);
  return entity.main(ctx);
}
