
// Generated by MonkEC - targeting Goja runtime
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __knownSymbol = (name, symbol) => (symbol = Symbol[name]) ? symbol : Symbol.for("Symbol." + name);
var __typeError = (msg) => {
  throw TypeError(msg);
};
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
var __decoratorStart = (base) => [, , , __create(base?.[__knownSymbol("metadata")] ?? null)];
var __decoratorStrings = ["class", "method", "getter", "setter", "accessor", "field", "value", "get", "set"];
var __expectFn = (fn) => fn !== void 0 && typeof fn !== "function" ? __typeError("Function expected") : fn;
var __decoratorContext = (kind, name, done, metadata, fns) => ({ kind: __decoratorStrings[kind], name, metadata, addInitializer: (fn) => done._ ? __typeError("Already initialized") : fns.push(__expectFn(fn || null)) });
var __decoratorMetadata = (array, target) => __defNormalProp(target, __knownSymbol("metadata"), array[3]);
var __runInitializers = (array, flags, self, value) => {
  for (var i = 0, fns = array[flags >> 1], n = fns && fns.length; i < n; i++) flags & 1 ? fns[i].call(self) : value = fns[i].call(self, value);
  return value;
};
var __decorateElement = (array, flags, name, decorators, target, extra) => {
  var fn, it, done, ctx, access, k = flags & 7, s = !!(flags & 8), p = !!(flags & 16);
  var j = k > 3 ? array.length + 1 : k ? s ? 1 : 2 : 0, key = __decoratorStrings[k + 5];
  var initializers = k > 3 && (array[j - 1] = []), extraInitializers = array[j] || (array[j] = []);
  var desc = k && (!p && !s && (target = target.prototype), k < 5 && (k > 3 || !p) && __getOwnPropDesc(k < 4 ? target : { get [name]() {
    return __privateGet(this, extra);
  }, set [name](x) {
    return __privateSet(this, extra, x);
  } }, name));
  k ? p && k < 4 && __name(extra, (k > 2 ? "set " : k > 1 ? "get " : "") + name) : __name(target, name);
  for (var i = decorators.length - 1; i >= 0; i--) {
    ctx = __decoratorContext(k, name, done = {}, array[3], extraInitializers);
    if (k) {
      ctx.static = s, ctx.private = p, access = ctx.access = { has: p ? (x) => __privateIn(target, x) : (x) => name in x };
      if (k ^ 3) access.get = p ? (x) => (k ^ 1 ? __privateGet : __privateMethod)(x, target, k ^ 4 ? extra : desc.get) : (x) => x[name];
      if (k > 2) access.set = p ? (x, y) => __privateSet(x, target, y, k ^ 4 ? extra : desc.set) : (x, y) => x[name] = y;
    }
    it = (0, decorators[i])(k ? k < 4 ? p ? extra : desc[key] : k > 4 ? void 0 : { get: desc.get, set: desc.set } : target, ctx), done._ = 1;
    if (k ^ 4 || it === void 0) __expectFn(it) && (k > 4 ? initializers.unshift(it) : k ? p ? extra = it : desc[key] = it : target = it);
    else if (typeof it !== "object" || it === null) __typeError("Object expected");
    else __expectFn(fn = it.get) && (desc.get = fn), __expectFn(fn = it.set) && (desc.set = fn), __expectFn(fn = it.init) && initializers.unshift(fn);
  }
  return k || __decoratorMetadata(array, target), desc && __defProp(target, name, desc), p ? k ^ 4 ? extra : desc : target;
};
var __publicField = (obj, key, value) => __defNormalProp(obj, typeof key !== "symbol" ? key + "" : key, value);
var __accessCheck = (obj, member, msg) => member.has(obj) || __typeError("Cannot " + msg);
var __privateIn = (member, obj) => Object(obj) !== obj ? __typeError('Cannot use the "in" operator on this value') : member.has(obj);
var __privateGet = (obj, member, getter) => (__accessCheck(obj, member, "read from private field"), getter ? getter.call(obj) : member.get(obj));
var __privateSet = (obj, member, value, setter) => (__accessCheck(obj, member, "write to private field"), setter ? setter.call(obj, value) : member.set(obj, value), value);
var __privateMethod = (obj, member, method) => (__accessCheck(obj, member, "access private method"), method);

// input/runpod/placement.ts
const runpodBase = require("runpod/runpod-base");
const RunpodEntity = runpodBase.RunpodEntity;
const action = runpodBase.action;
const common = require("runpod/common");
const rankByAvailability = common.rankByAvailability;
const cli = require("cli");
var _getInfo_dec, _a, _init;
var _RunpodPlacement = class _RunpodPlacement extends (_a = RunpodEntity, _getInfo_dec = [action("get-info")], _a) {
  constructor() {
    super(...arguments);
    __runInitializers(_init, 5, this);
  }
  getEntityName() {
    return `RunPod placement (${this.definition.gpu_type_id ?? this.definition.cpu_flavor_id ?? "unresolved"})`;
  }
  create() {
    if (this.state.data_center) {
      cli.output(`Placement already resolved to ${this.state.data_center}; resolution is frozen once made.`);
      return;
    }
    const gpuTypeId = this.definition.gpu_type_id;
    const cpuFlavorId = this.definition.cpu_flavor_id;
    if (!gpuTypeId && !cpuFlavorId) {
      throw new Error("runpod-placement needs exactly one of gpu_type_id or cpu_flavor_id.");
    }
    const product = this.definition.product || "POD";
    const result = gpuTypeId ? this.gpuAvailability(gpuTypeId, product, this.definition.cloud, this.definition.count) : this.cpuAvailability(cpuFlavorId, product);
    if (!result) {
      throw new Error(`No catalog entry found for ${gpuTypeId ?? cpuFlavorId} (product=${product}).`);
    }
    let candidates = (result.dataCenters ?? []).slice();
    if (this.definition.volume_type) {
      const compatible = this.catalogDatacenters(this.definition.volume_type);
      const compatibleIds = new Set(compatible.map((dc) => dc.id));
      candidates = candidates.filter((c) => compatibleIds.has(c.id));
    }
    if (this.definition.candidate_data_center_ids && this.definition.candidate_data_center_ids.length > 0) {
      const allow = new Set(this.definition.candidate_data_center_ids);
      candidates = candidates.filter((c) => allow.has(c.id));
    }
    const ranked = rankByAvailability(candidates).filter((c) => c.availability !== "NONE");
    this.state.candidates = ranked;
    if (ranked.length === 0) {
      throw new Error(
        `No datacenter has ${gpuTypeId ?? cpuFlavorId} available` + (this.definition.volume_type ? ` with ${this.definition.volume_type} volume support` : "") + (this.definition.candidate_data_center_ids ? ` within [${this.definition.candidate_data_center_ids.join(", ")}]` : "") + ". Try again later, widen candidate_data_center_ids, or drop volume_type."
      );
    }
    const chosen = ranked[0];
    this.state.data_center = chosen.id;
    this.state.data_center_name = chosen.name;
    this.state.availability = chosen.availability;
    cli.output(`\u2705 Resolved placement: ${chosen.id} (${chosen.availability})`);
  }
  update() {
    if (this.state.data_center) {
      cli.output(
        `Placement already resolved to ${this.state.data_center}; definition changes are ignored. Delete and recreate this entity to re-resolve.`
      );
      return;
    }
    this.create();
  }
  delete() {
    cli.output("RunPod placement entity has no remote resources to delete; clearing resolution.");
    this.state.data_center = void 0;
    this.state.data_center_name = void 0;
    this.state.availability = void 0;
    this.state.candidates = void 0;
  }
  checkReadiness() {
    return Boolean(this.state.data_center);
  }
  getInfo(_args) {
    if (!this.state.data_center) {
      cli.output("Placement not resolved yet");
      return;
    }
    cli.output("=== Placement ===");
    cli.output(`Resolved: ${this.state.data_center} (${this.state.data_center_name ?? ""}) \u2014 ${this.state.availability}`);
    cli.output("Candidates considered:");
    for (const c of this.state.candidates ?? []) {
      cli.output(`  ${c.id} \u2014 ${c.availability}`);
    }
  }
};
_init = __decoratorStart(_a);
__decorateElement(_init, 1, "getInfo", _getInfo_dec, _RunpodPlacement);
__decoratorMetadata(_init, _RunpodPlacement);
__name(_RunpodPlacement, "RunpodPlacement");
__publicField(_RunpodPlacement, "readiness", { period: 2, initialDelay: 1, attempts: 10 });
var RunpodPlacement = _RunpodPlacement;



function main(def, state, ctx) {
  const entity = new RunpodPlacement(def, state, ctx);
  return entity.main(ctx);
}
