
// Generated by MonkEC - targeting Goja runtime
var __defProp = Object.defineProperty;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });

// input/workos/credentials.ts
const workosBase = require("workos/workos-base");
const WorkOSEntity = workosBase.WorkOSEntity;
const cli = require("cli");
var _Credentials = class _Credentials extends WorkOSEntity {
  getEntityName() {
    return "workos-credentials";
  }
  create() {
    const mode = this.deriveMode();
    this.makeRequest("GET", "/organizations?limit=1");
    this.state = {
      mode,
      client_id: this.definition.client_id,
      secret_ref: this.definition.secret_ref,
      existing: false
    };
    cli.output(`WorkOS credentials valid (mode: ${mode})`);
  }
  update() {
    this.create();
  }
  delete() {
    cli.output("WorkOS credentials entity has no remote resources to delete");
  }
  checkReadiness() {
    return Boolean(this.state?.secret_ref);
  }
};
__name(_Credentials, "Credentials");
var Credentials = _Credentials;



function main(def, state, ctx) {
  const entity = new Credentials(def, state, ctx);
  return entity.main(ctx);
}
