// Generated by MonkEC - targeting Goja runtime
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var __publicField = (obj, key, value) => __defNormalProp(obj, typeof key !== "symbol" ? key + "" : key, value);

// input/aws-glue-schema-registry/glueSchemaRegistryBase.ts
var glue_schema_registry_base_exports = {};
__export(glue_schema_registry_base_exports, {
  AWSGlueSchemaRegistryEntity: () => AWSGlueSchemaRegistryEntity,
  action: () => import_base.action
});
module.exports = __toCommonJS(glue_schema_registry_base_exports);
var import_base = require("monkec/base");
var import_aws = __toESM(require("cloud/aws"));
var AWSGlueSchemaRegistryEntity = class extends import_base.MonkEntity {
  constructor() {
    super(...arguments);
    __publicField(this, "region");
  }
  before() {
    this.region = this.definition.region;
  }
  /**
   * Make a request to the AWS Glue API using JSON-RPC style
   * @param action - The Glue API action (e.g., "CreateRegistry")
   * @param body - The request body as an object
   * @returns Parsed JSON response
   */
  makeGlueRequest(action2, body) {
    const url = `https://glue.${this.region}.amazonaws.com/`;
    const options = {
      service: "glue",
      region: this.region,
      headers: {
        "X-Amz-Target": `AWSGlue.${action2}`,
        "Content-Type": "application/x-amz-json-1.1"
      },
      body: JSON.stringify(body),
      timeout: 30
    };
    try {
      const response = import_aws.default.post(url, options);
      if (response.statusCode >= 400) {
        let errorMessage = `Glue API error: ${response.statusCode} ${response.status}`;
        try {
          const errorBody = JSON.parse(response.body);
          const message = errorBody.Message || errorBody.message;
          if (message) {
            errorMessage += ` - ${message}`;
          }
          if (errorBody.__type) {
            const exceptionName = errorBody.__type.split("#").pop() || errorBody.__type;
            errorMessage += ` (${exceptionName})`;
          }
        } catch (_parseError) {
          errorMessage += ` - Raw: ${response.body}`;
        }
        throw new Error(errorMessage);
      }
      if (response.body) {
        try {
          return JSON.parse(response.body);
        } catch (_error) {
          return {};
        }
      }
      return {};
    } catch (error) {
      if (error instanceof Error && error.message.startsWith("Glue API error:")) {
        throw error;
      }
      throw new Error(`Glue API request failed: ${error instanceof Error ? error.message : "Unknown error"}`);
    }
  }
  /**
   * Check if an error indicates the resource was not found
   */
  isNotFoundError(error) {
    if (error instanceof Error) {
      return error.message.includes("EntityNotFoundException") || error.message.includes("NotFoundException");
    }
    return false;
  }
  /**
   * Check if an error indicates the resource already exists
   */
  isAlreadyExistsError(error) {
    if (error instanceof Error) {
      return error.message.includes("AlreadyExistsException");
    }
    return false;
  }
  /**
   * Simple delay implementation using busy wait
   */
  delay(seconds) {
    const delayMs = seconds * 1e3;
    const start = Date.now();
    while (Date.now() - start < delayMs) {
    }
  }
};
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  AWSGlueSchemaRegistryEntity,
  action
});
