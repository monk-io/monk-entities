
// Generated by MonkEC - targeting Goja runtime
var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __knownSymbol = (name, symbol) => (symbol = Symbol[name]) ? symbol : Symbol.for("Symbol." + name);
var __typeError = (msg) => {
  throw TypeError(msg);
};
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __name = (target, value) => __defProp(target, "name", { value, configurable: true });
var __decoratorStart = (base) => [, , , __create(base?.[__knownSymbol("metadata")] ?? null)];
var __decoratorStrings = ["class", "method", "getter", "setter", "accessor", "field", "value", "get", "set"];
var __expectFn = (fn) => fn !== void 0 && typeof fn !== "function" ? __typeError("Function expected") : fn;
var __decoratorContext = (kind, name, done, metadata, fns) => ({ kind: __decoratorStrings[kind], name, metadata, addInitializer: (fn) => done._ ? __typeError("Already initialized") : fns.push(__expectFn(fn || null)) });
var __decoratorMetadata = (array, target) => __defNormalProp(target, __knownSymbol("metadata"), array[3]);
var __runInitializers = (array, flags, self, value) => {
  for (var i = 0, fns = array[flags >> 1], n = fns && fns.length; i < n; i++) flags & 1 ? fns[i].call(self) : value = fns[i].call(self, value);
  return value;
};
var __decorateElement = (array, flags, name, decorators, target, extra) => {
  var fn, it, done, ctx, access, k = flags & 7, s = !!(flags & 8), p = !!(flags & 16);
  var j = k > 3 ? array.length + 1 : k ? s ? 1 : 2 : 0, key = __decoratorStrings[k + 5];
  var initializers = k > 3 && (array[j - 1] = []), extraInitializers = array[j] || (array[j] = []);
  var desc = k && (!p && !s && (target = target.prototype), k < 5 && (k > 3 || !p) && __getOwnPropDesc(k < 4 ? target : { get [name]() {
    return __privateGet(this, extra);
  }, set [name](x) {
    return __privateSet(this, extra, x);
  } }, name));
  k ? p && k < 4 && __name(extra, (k > 2 ? "set " : k > 1 ? "get " : "") + name) : __name(target, name);
  for (var i = decorators.length - 1; i >= 0; i--) {
    ctx = __decoratorContext(k, name, done = {}, array[3], extraInitializers);
    if (k) {
      ctx.static = s, ctx.private = p, access = ctx.access = { has: p ? (x) => __privateIn(target, x) : (x) => name in x };
      if (k ^ 3) access.get = p ? (x) => (k ^ 1 ? __privateGet : __privateMethod)(x, target, k ^ 4 ? extra : desc.get) : (x) => x[name];
      if (k > 2) access.set = p ? (x, y) => __privateSet(x, target, y, k ^ 4 ? extra : desc.set) : (x, y) => x[name] = y;
    }
    it = (0, decorators[i])(k ? k < 4 ? p ? extra : desc[key] : k > 4 ? void 0 : { get: desc.get, set: desc.set } : target, ctx), done._ = 1;
    if (k ^ 4 || it === void 0) __expectFn(it) && (k > 4 ? initializers.unshift(it) : k ? p ? extra = it : desc[key] = it : target = it);
    else if (typeof it !== "object" || it === null) __typeError("Object expected");
    else __expectFn(fn = it.get) && (desc.get = fn), __expectFn(fn = it.set) && (desc.set = fn), __expectFn(fn = it.init) && initializers.unshift(fn);
  }
  return k || __decoratorMetadata(array, target), desc && __defProp(target, name, desc), p ? k ^ 4 ? extra : desc : target;
};
var __publicField = (obj, key, value) => __defNormalProp(obj, typeof key !== "symbol" ? key + "" : key, value);
var __accessCheck = (obj, member, msg) => member.has(obj) || __typeError("Cannot " + msg);
var __privateIn = (member, obj) => Object(obj) !== obj ? __typeError('Cannot use the "in" operator on this value') : member.has(obj);
var __privateGet = (obj, member, getter) => (__accessCheck(obj, member, "read from private field"), getter ? getter.call(obj) : member.get(obj));
var __privateSet = (obj, member, value, setter) => (__accessCheck(obj, member, "write to private field"), setter ? setter.call(obj, value) : member.set(obj, value), value);
var __privateMethod = (obj, member, method) => (__accessCheck(obj, member, "access private method"), method);

// input/cloudflare/queue.ts
const base = require("monkec/base");
const action = base.action;
const cli = require("cli");
const cloudflareBase = require("cloudflare/cloudflare-base");
const CloudflareEntity = cloudflareBase.CloudflareEntity;
var _forceDelete_dec, _drainMessages_dec, _pullMessages_dec, _sendMessage_dec, _getInfo_dec, _a, _init;
var _CloudflareQueue = class _CloudflareQueue extends (_a = CloudflareEntity, _getInfo_dec = [action("get-info")], _sendMessage_dec = [action("send-message")], _pullMessages_dec = [action("pull-messages")], _drainMessages_dec = [action("drain-messages")], _forceDelete_dec = [action("force-delete")], _a) {
  constructor() {
    super(...arguments);
    __runInitializers(_init, 5, this);
  }
  create() {
    const accountId = this.definition.account_id;
    const name = this.definition.name;
    const existing = this.findQueueByName(accountId, name);
    if (existing?.queue_id) {
      this.state = {
        id: existing.queue_id,
        name: existing.queue_name || name,
        created_on: existing.created_on,
        modified_on: existing.modified_on,
        existing: true
      };
      cli.output(`\u{1F4EC} Adopted existing Queue ${name} (${existing.queue_id})`);
      this.applySettings();
      return;
    }
    const body = { queue_name: name };
    if (this.definition.settings) body.settings = this.definition.settings;
    const created = this.request(
      "POST",
      `/accounts/${accountId}/queues`,
      body
    );
    const result = created?.result || {};
    this.state = {
      id: result.queue_id,
      name: result.queue_name || name,
      created_on: result.created_on,
      modified_on: result.modified_on,
      existing: false
    };
    cli.output(`\u2705 Created Queue ${name} (${result.queue_id})`);
  }
  update() {
    if (!this.state.id) {
      this.create();
      return;
    }
    this.applySettings();
    const accountId = this.definition.account_id;
    try {
      const res = this.request(
        "GET",
        `/accounts/${accountId}/queues/${this.state.id}`
      );
      const q = res?.result;
      if (q?.modified_on) this.state.modified_on = q.modified_on;
    } catch {
    }
  }
  delete() {
    if (!this.state.id) return;
    if (this.state.existing) {
      cli.output("Queue existed before this entity; skipping delete");
      return;
    }
    if (!this.definition.allow_destructive_delete) {
      cli.output(
        `Queue ${this.state.id} delete is disabled. Set allow_destructive_delete: true or invoke the force-delete action to remove it.`
      );
      return;
    }
    this.destroyQueue();
  }
  checkReadiness() {
    return Boolean(this.state.id);
  }
  getInfo() {
    if (!this.state.id) {
      cli.output("No queue yet");
      return;
    }
    const accountId = this.definition.account_id;
    const res = this.request(
      "GET",
      `/accounts/${accountId}/queues/${this.state.id}`
    );
    cli.output(JSON.stringify(res?.result || {}, null, 2));
  }
  sendMessage(args) {
    if (!this.state.id) {
      cli.output("No queue yet");
      return;
    }
    const accountId = this.definition.account_id;
    const message = args?.message ?? `monk-test ${Date.now()}`;
    const body = {
      messages: [
        { body: String(message), content_type: "text" }
      ]
    };
    const res = this.request(
      "POST",
      `/accounts/${accountId}/queues/${this.state.id}/messages/batch`,
      body
    );
    cli.output(`\u{1F4E4} Sent message: ${String(message)}`);
    cli.output(JSON.stringify(res?.result || res || {}, null, 2));
  }
  pullMessages(args) {
    if (!this.state.id) {
      cli.output("No queue yet");
      return;
    }
    const accountId = this.definition.account_id;
    const batchSize = Number(args?.batch_size ?? 25);
    const visibilityMs = Number(args?.visibility_timeout_ms ?? 5e3);
    const res = this.request(
      "POST",
      `/accounts/${accountId}/queues/${this.state.id}/messages/pull`,
      { batch_size: batchSize, visibility_timeout_ms: visibilityMs }
    );
    const messages = res?.result?.messages || [];
    cli.output(`\u{1F4E5} Pulled ${messages.length} message(s)`);
    for (const m of messages) {
      cli.output(`  - id=${m.id} body=${JSON.stringify(m.body)}`);
    }
    cli.output(JSON.stringify(res?.result || {}, null, 2));
  }
  drainMessages() {
    if (!this.state.id) {
      cli.output("No queue yet");
      return;
    }
    const accountId = this.definition.account_id;
    let total = 0;
    for (let i = 0; i < 20; i++) {
      const res = this.request(
        "POST",
        `/accounts/${accountId}/queues/${this.state.id}/messages/pull`,
        { batch_size: 100, visibility_timeout_ms: 1e3 }
      );
      const messages = res?.result?.messages || [];
      if (messages.length === 0) break;
      total += messages.length;
      const acks = messages.map((m) => ({ lease_id: m.lease_id }));
      try {
        this.request(
          "POST",
          `/accounts/${accountId}/queues/${this.state.id}/messages/ack`,
          { acks, retries: [] }
        );
      } catch (e) {
        cli.output(`ack failed: ${e?.message || e}`);
        break;
      }
    }
    cli.output(`\u{1F9F9} Purged ${total} message(s)`);
  }
  forceDelete() {
    if (!this.state.id) {
      cli.output("No queue to delete");
      return;
    }
    if (this.state.existing) {
      cli.output(
        `Refusing force-delete: queue ${this.state.id} pre-existed and was adopted.`
      );
      return;
    }
    this.destroyQueue();
  }
  applySettings() {
    if (!this.definition.settings || !this.state.id) return;
    const accountId = this.definition.account_id;
    try {
      this.request(
        "PATCH",
        `/accounts/${accountId}/queues/${this.state.id}`,
        { settings: this.definition.settings }
      );
    } catch (e) {
      cli.output(`Queue settings PATCH failed: ${e?.message || e}`);
    }
  }
  destroyQueue() {
    const accountId = this.definition.account_id;
    try {
      this.request("DELETE", `/accounts/${accountId}/queues/${this.state.id}`);
      cli.output(`\u{1F5D1}\uFE0F Deleted Queue ${this.state.id}`);
    } catch (e) {
      const msg = e?.message || String(e);
      if (msg.includes("404")) {
        cli.output(`Queue ${this.state.id} already gone`);
        return;
      }
      throw e;
    }
  }
  findQueueByName(accountId, name) {
    try {
      const res = this.request(
        "GET",
        `/accounts/${accountId}/queues`
      );
      const queues = res?.result || [];
      for (const q of queues) {
        if (q?.queue_name === name && q?.queue_id) return q;
      }
      return null;
    } catch {
      return null;
    }
  }
};
_init = __decoratorStart(_a);
__decorateElement(_init, 1, "getInfo", _getInfo_dec, _CloudflareQueue);
__decorateElement(_init, 1, "sendMessage", _sendMessage_dec, _CloudflareQueue);
__decorateElement(_init, 1, "pullMessages", _pullMessages_dec, _CloudflareQueue);
__decorateElement(_init, 1, "drainMessages", _drainMessages_dec, _CloudflareQueue);
__decorateElement(_init, 1, "forceDelete", _forceDelete_dec, _CloudflareQueue);
__decoratorMetadata(_init, _CloudflareQueue);
__name(_CloudflareQueue, "CloudflareQueue");
__publicField(_CloudflareQueue, "readiness", { period: 5, initialDelay: 1, attempts: 6 });
var CloudflareQueue = _CloudflareQueue;



function main(def, state, ctx) {
  const entity = new CloudflareQueue(def, state, ctx);
  return entity.main(ctx);
}
